## 2024-05-22 - Form Label Association Gap
**Learning:** The app's `ControlPanel` and `SettingsModal` frequently place labels visually near inputs but lack programmatic association (htmlFor/id), reducing accessibility for screen readers and click targeting.
**Action:** Enforce strict `htmlFor` and `id` pairing on all form inputs during implementation or review.
